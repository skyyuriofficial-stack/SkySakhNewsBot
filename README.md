# SkySakhNews Bot

Полностью автоматический Telegram-публикатор новостей на GitHub Actions. Телефон, Termux, постоянно включённый компьютер и отдельный VPS не требуются.

## Production

Текущая версия: **stable-v10.2**.

Единственный плановый publisher:

```text
.github/workflows/auto_publish_v7.yml
        ↓
src/editorial_gate_runner.py
        ↓
src/production.py
        ↓
src/news_bot_v9.py / src/news_bot_v8.py
        ↓
Telegram Bot API
```

Перед Telegram каждый материал проходит независимую проверку:

```text
источник → свежесть → прямая ссылка → первичная категория
→ title-first category reconciler → source/category pre-gate
→ перевод/редактура → title/source semantic gate
→ проверка фактов, чисел и модальности → публикация
```

## Что проверяется

- новость не старше 36 часов;
- итоговая ссылка ведёт на оригинальную статью, а не на Google News;
- география определяется по смыслу новости, а не по домену СМИ;
- заголовок отражает центральную суть источника;
- `может/планирует/обсуждает` не превращается в `сделал/ввёл/принял`;
- отсутствуют придуманные числа и факты;
- категория соответствует заголовку и статье;
- землетрясение сохраняет имеющиеся в источнике магнитуду, глубину, расстояние, балльность и время;
- одинаковые URL, заголовки, картинки и сюжетные кластеры не повторяются;
- логотипы, баннеры и текстовые карточки не используются как фотографии новости;
- если безопасной картинки нет, допускается корректный текстовый пост;
- если OpenRouter недоступен, русскоязычная новость может пройти только через дословный extractive fallback; неподтверждённый рерайт не публикуется.

## Потоки

- `📍 Сахалин` — локальные события, ЧП и сейсмика;
- `🌍 Мир о России` — материалы авторитетных мировых СМИ, где Россия является предметом новости;
- `🇷🇺 Россия / безопасность`;
- `🇷🇺 Россия / происшествия`;
- `🇷🇺 Россия / политика`;
- `🇷🇺 Россия / экономика`;
- `🧭 Геополитика`;
- `💻 IT / технологии`.

Первый пост выпуска — локальный, когда существует прошедший все проверки сахалинский кандидат. Второй выбирается из другого приоритетного потока.

## Источники

Локальные:

- SakhalinMedia RSS;
- ASTV HTML adapter;
- Sakh.online HTML adapter;
- Sakhalin Google News как discovery-источник с обязательным разрешением прямой ссылки.

Россия и мир:

- Interfax;
- TASS;
- Reuters;
- AP News;
- BBC World;
- The Guardian World;
- BBC Technology;
- The Guardian Technology.

## Расписание

Сахалин: UTC+11.

```text
07:00
10:00
13:00
16:00
19:00
22:00
```

Cron GitHub Actions:

```text
0 2,5,8,11,20,23 * * *
```

За цикл публикуется до двух материалов. Ноль или один пост допустимы, если остальные кандидаты не прошли проверку качества.

## Secrets

В `Settings → Secrets and variables → Actions`:

- `TELEGRAM_BOT_TOKEN`;
- `TELEGRAM_CHANNEL_ID`;
- `OPENROUTER_API_KEY`;
- `OPENROUTER_MODEL` — необязательно; при пустом значении используется бесплатный маршрут OpenRouter.

## Контроль качества

- `production_ci.yml` — компиляция и регрессионные тесты без публикации;
- `system_smoke_test.yml` — ручная техническая проверка;
- `editorial_audit_report.json` — результат последней расширенной проверки на живых источниках;
- `state.json` — история публикаций, дедупликация и health-status последнего запуска.

Последняя end-to-end проверка stable-v10.2 прошла на живых источниках, после чего два материала были реально опубликованы и автоматически проверены по `title/source`, категории, фактам и прямым ссылкам.
