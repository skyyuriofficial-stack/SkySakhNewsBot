name: SkySakhNews Publish Approved Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy editorial publisher disabled
        run: |
          echo "Legacy approved-draft publisher is disabled."
          echo "It must not publish from editorial_queue.json."
          echo "Production is handled only by SkySakhNews Auto Publisher Stable v9.4."
