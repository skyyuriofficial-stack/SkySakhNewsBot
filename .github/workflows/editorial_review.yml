name: SkySakhNews Editorial Review Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy editorial review disabled
        run: |
          echo "Legacy editorial review automation is disabled."
          echo "It must not mutate editorial_queue.json automatically."
          echo "Production is handled only by SkySakhNews Auto Publisher Stable v9.6."
