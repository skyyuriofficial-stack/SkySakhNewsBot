name: SkySakhNews Minimum Release Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy minimum-release publisher disabled
        run: |
          echo "Legacy minimum-release publisher is disabled."
          echo "It must not publish after another workflow completes."
          echo "Production is handled only by SkySakhNews Auto Publisher Stable v9.4."
