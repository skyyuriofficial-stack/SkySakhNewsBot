name: SkySakhNews Direct Publisher Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Direct publisher disabled
        run: |
          echo "Direct publisher is disabled to prevent unreviewed posts."
          echo "Use SkySakhNews Editorial Cycle instead."
          exit 0
