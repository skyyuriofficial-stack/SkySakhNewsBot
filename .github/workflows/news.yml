name: SkySakhNews Legacy Direct Publisher Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy direct publisher disabled
        run: |
          echo "Legacy direct publisher is disabled."
          echo "Production is handled only by SkySakhNews Auto Publisher Stable v9.5."
          exit 0
