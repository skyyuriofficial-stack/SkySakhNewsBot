name: SkySakhNews Cycle Report Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy cycle report disabled
        run: |
          echo "Legacy editorial cycle reporting automation is disabled."
          echo "It must not mutate state.json or editorial_queue.json automatically."
          echo "Production health is recorded by SkySakhNews Auto Publisher Stable v9.6."
