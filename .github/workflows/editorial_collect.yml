name: SkySakhNews Editorial Collect Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy editorial collector disabled
        run: |
          echo "Legacy editorial collector is disabled."
          echo "Production is handled only by SkySakhNews Auto Publisher Stable v9.4."
