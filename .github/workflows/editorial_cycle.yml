name: SkySakhNews Editorial Cycle Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy editorial cycle disabled
        run: |
          echo "Legacy editorial cycle is disabled."
          echo "It must not collect, review, publish, audit, or mutate state automatically."
          echo "Production is handled only by SkySakhNews Auto Publisher Stable v9.4."
